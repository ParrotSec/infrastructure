# Fail2Ban configuration file
[Definition]
 
# Option: failregex
failregex = ^ - <HOST> \<-\> \<-\> \<(404|500)\>> \<\d+\> \<(deb|download|mirror|archive)\.([a-z0-9]|\.)+\> .+$
            ^ - \<((\d+|[a-zA-Z]+|\.|:)+)\> <HOST> \<([a-zA-Z0-9]+|-)\> \<(404|500)\> \<\d+\> \<(deb|download|mirror|archive)\.([a-z0-9]|\.)+\> .+$
# Option: ignoreregex
ignoreregex =
